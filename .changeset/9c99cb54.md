---
"hex-app": patch
---

Fix global hotkey stops responding after CGEventTap is disabled by macOS (timeout/userInput); the tap is now re-enabled instead of being blocked by a stale isMonitoring flag
